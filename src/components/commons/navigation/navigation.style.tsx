import { Stack, type StackProps, styled } from '@mui/material';

export const NavigationContainer = styled(Stack)<StackProps>({
  backgroundColor: '#FFD600',
  padding: '8px 0',
  margin: '16px 0',
});
